export enum TransactionStatus {
  SUCCESS = 'success',
  FAIL = 'fail',
  CANCELLED = 'cancel',
  ON_HOLD = 'on hold',
  REFUNDED = 'refunded',
  // add unPaid / Paid
}

// TODO move pok api into external dir
